#pragma once
#include <Arduino.h>

// Sampling
static const uint16_t SAMPLE_COUNT    = 50;
static const uint16_t SAMPLE_DELAY_MS = 2;

// Buttons
static const uint16_t DEBOUNCE_MS  = 35;
static const uint16_t LONGPRESS_MS = 900;

// Serial
static const uint32_t SERIAL_BAUD = 115200;
